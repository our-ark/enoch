# Benchmarks

This directory contains Enoch's supported, versioned benchmark releases.

- [`pai-bench`](pai-bench/) evaluates persistent identity in deployed AI
  agents through provider-neutral target and evaluator adapters.
